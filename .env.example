# HostCraft Environment Configuration Example
# This file shows the environment variables that can be set for HostCraft
# SECURITY: Never commit actual .env files with real values to version control!
#
# NOTE: These values are auto-generated during installation by install.sh
# The install script exports these as environment variables directly for security.
# You typically don't need to set them manually unless overriding defaults.

# Port Configuration (can be overridden)
WEB_PORT=5000
API_PORT=5100

# Database Configuration (auto-generated secure password during install)
POSTGRES_DB=hostcraft
POSTGRES_USER=hostcraft
POSTGRES_PASSWORD=auto_generated_secure_password

# Encryption Key (REQUIRED - auto-generated 256-bit key during install)
ENCRYPTION_KEY=auto_generated_base64_encryption_key

# API Configuration (for Web service - auto-configured)
# ApiUrl=http://hostcraft-api:8080

# Localhost Server Configuration (auto-configured during install)
SKIP_LOCALHOST_SEED=false
LOCALHOST_IS_SWARM_MANAGER=false

# Traefik/SSL Configuration (optional - can be configured via UI)
# TRAEFIK_DOMAIN=hostcraft.yourdomain.com
# TRAEFIK_EMAIL=admin@yourdomain.com
